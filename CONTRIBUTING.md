# Nanachi 的台股監控看板 貢獻、AI 開發與複審指南

本文檔適用於整個倉庫，供貢獻者、AI 編碼代理和 PR 審查者共同使用。目標是讓改動落在正確的模塊，保持數據口徑、插件化和兼容性一致，並通過可復現的驗證減少返工。

`CONTRIBUTING.md` 是項目貢獻與審查規範，不替代 `README.md`、`docs/` 中的用戶文檔和領域文檔。所有貢獻者和 AI 編碼代理在修改代碼、提交或審查 PR 前都應閱讀本文檔。

涉及代碼二次開發時，同時遵循 [`docs/secondary-development.md`](docs/secondary-development.md)。前端優先使用真實存在的受控插槽或註冊入口，後端優先使用小粒度策略接口和依賴注入；現有擴展點無法表達核心行為變化時允許直接修改源碼，但必須保持改動聚焦並補足兼容性驗證。

## 1. 基本原則

### 1.1 修改前先理解

- 先定位真實調用鏈、數據來源、緩存層和現有測試，不根據文件名或界面現象猜測實現。
- 明確用戶問題、輸入、輸出、資產類型、時間週期和兼容邊界後再修改。
- 存在多種解釋時必須說明假設；會改變業務口徑的歧義必須先確認。
- 優先複用現有接口、服務、類型和組件，不平行實現第二套邏輯。

### 1.2 保持簡單

- 只實現當前需求，不增加未被要求的配置、兼容層或通用框架。
- 單次使用的簡單邏輯不提前抽象；出現真實重複或明確模塊邊界時再複用。
- 能在現有模塊內完成的改動，不新增跨層依賴或新目錄。
- 註釋解釋“為什麼”，不復述代碼行為。

### 1.3 最小範圍修改

- 每一處變更都應能追溯到當前問題。
- 不順手重構、重命名、格式化或刪除無關代碼。
- 保留用戶和其他貢獻者尚未提交的改動，不覆蓋、不回滾。
- 只清理由本次修改產生的無用導入、變量、文件和調試輸出。

### 1.4 以驗證結果作為完成標準

- 修復 bug 時，優先編寫能復現問題的測試，再修到測試通過。
- 新功能至少覆蓋正常路徑、關鍵邊界和失敗路徑。
- “代碼已寫完”“頁面能打開”或“GitHub 沒有紅燈”都不代表完成。
- 無法執行必要驗證時，必須在 PR 中說明原因和剩餘風險。

## 2. 項目架構

### 2.1 技術棧

- 後端：Python 3.11+、FastAPI、Pydantic v2、Polars、DuckDB、PyArrow。
- 回測邊界允許使用 pandas；業務數據計算優先使用 Polars。`vectorbt` 是可選依賴，不得讓未安裝它的主流程失效。
- 前端：React 18、TypeScript、Vite、TanStack Query、Tailwind CSS、ECharts、lightweight-charts。
- 包管理和驗證命令：後端使用 `uv`，前端使用 `pnpm`。

### 2.2 主數據流

```text
數據源或數據插件
  -> services 同步、校驗與標準化
  -> DataStore / KlineRepository / Parquet
  -> indicators pipeline 生成 enriched 數據
  -> 策略 / 監控 / 回測 / 分析服務
  -> FastAPI API / SSE
  -> frontend API 類型 + TanStack Query
  -> 頁面和共享組件
```

改動前應沿這條鏈路檢查上下游。禁止通過頁面或 API 層繞過標準化、倉庫、業務服務而直接讀取某個數據源或本地文件。

### 2.3 模塊邊界

| 目錄 | 職責 | 放置要求 |
| --- | --- | --- |
| `backend/app/api/` | HTTP、SSE、參數校驗和響應映射 | 保持薄層，不放重計算、全量掃描或數據源專屬業務 |
| `backend/app/services/` | 同步、實時行情、通知等業務編排 | 組合領域能力，不複製倉庫或 provider 的實現 |
| `backend/app/tickflow/` | 數據存儲、倉庫、能力檢測和 TickFlow 接入 | 對外通過既有抽象暴露能力，避免上層依賴存儲細節 |
| `backend/app/data_providers/` | 數據源接口、標準化模型和自定義數據源 | 新數據能力先定義數據集契約，再由 provider 實現 |
| `backend/app/plugins/` | 插件發現、加載和插件協議 | 插件失敗不得破壞未啟用插件的主流程 |
| `backend/app/indicators/` | 指標計算和 enriched 流水線 | 保持向量化，明確原始價、復權價和輸出單位 |
| `backend/app/strategy/` | 策略註冊、執行、監控和 AI 策略 | 內置與自定義策略儘量共享執行契約，不能各自形成不同口徑 |
| `backend/app/backtest/` | 回測、優化、步進優化和 worker | 嚴格隔離信號生成與成交模擬，防止未來函數 |
| `frontend/src/lib/api.ts` | 前端 API 請求和類型契約 | 後端契約變化必須同步更新類型與調用方 |
| `frontend/src/lib/queryKeys.ts` | TanStack Query 查詢鍵 | 查詢鍵集中維護，不在組件內拼出平行鍵 |
| `frontend/src/lib/useSharedQueries.ts` 等 | 跨頁面共享查詢 | 相同數據優先共享緩存，避免每張卡片重複請求 |
| `frontend/src/pages/` | 頁面級編排 | 複雜領域邏輯下沉到 hook、組件或後端服務 |
| `frontend/src/components/` | 可複用交互組件 | 保持 props 契約穩定，不把頁面專屬狀態塞進公共組件 |
| `data/` | 用戶運行時數據 | 不提交到 Git，不把本地樣本、賬號信息或絕對路徑寫入 PR |

## 3. 不可混用的數據契約

金融數據錯誤經常不會報異常，但會生成看似合理的錯誤結果。涉及下列字段時，必須在變量名、邊界轉換或測試中證明口徑正確。

### 3.1 比例和百分比

| 字段/場景 | 當前口徑 | 示例 |
| --- | --- | --- |
| 自定義實時數據源入口的 `change_pct` | 小數制 | `0.0366` 表示 `3.66%` |
| enriched 和股票監控內部的 `change_pct` | 通常為小數制 | 閾值比較前確認調用鏈，不憑字段名推斷 |
| 指數實時展示緩存的漲跌幅 | 存在百分數口徑 | 使用前必須顯式轉換，不得直接複用到股票監控 |
| 實時數據源入口的 `turnover_rate` | 小數制 | `0.05` 表示 `5%` |
| enriched 的 `turnover_rate` | 百分數值 | `5` 表示 `5%` |

新增跨邊界映射時必須增加單位測試。禁止用“數值小於 1 就乘 100”一類啟發式轉換，這會掩蓋真實數據錯誤。

### 3.2 價格與復權

- enriched 的 `open/high/low/close` 為前復權價格。
- `raw_close/raw_high/raw_low` 為不復權原始價格。
- 漲停、跌停、一字板、炸板和交易所價格限制必須基於原始價及對應交易日規則判斷。
- 技術指標和收益序列使用哪種價格，必須與現有指標定義一致，不得在同一公式中混用。

### 3.3 日期、交易日和時區

- 窗口、前 N 日和批次回算均按實際交易日，不得用自然日直接替代。
- A 股交易時段統一按北京時間處理；服務器時區不能成為業務邏輯的隱式輸入。
- 日線、分鐘線和實時快照必須明確交易日期歸屬，尤其注意午休、收盤後和跨日重啟。
- 分鐘 K 的股票、ETF、指數分開存儲和路由，不得僅憑代碼格式猜測資產類型。

### 3.4 歷史股本與換手率

- 歷史換手率優先使用公告日不晚於目標交易日的歷史股本。
- 缺少可用歷史股本時，才允許降級到最新維表股本。
- 當日維表、財務公告日和報表期是不同概念，不能用報表期提前洩露尚未公告的數據。
- 修改股本補全或 enriched 重算時，必須覆蓋“有歷史股本”和“無歷史股本降級”兩條路徑。

## 4. 數據源插件化要求

數據源已經插件化。任何通用功能都必須通過 provider 能力和標準化數據集訪問數據，不能把 TickFlow SDK 調用硬編碼到策略、監控、回測、API 或前端流程中。

- 使用現有的 `get_provider()`、`provider_has_dataset()` 和 preferences 路由能力。
- 支持的數據集包括但不限於 `daily`、`adj_factor`、`minute`、`realtime`、`financial`；新增數據集應先定義清晰的輸入輸出契約。
- provider 負責把供應商字段、單位、日期和代碼格式轉換為內部標準格式。
- 上層服務依賴標準字段和能力聲明，不依賴供應商響應結構。
- 只有明確標註為 TickFlow 專屬的功能才可以直接依賴 TickFlow，並且不得影響其他 provider。
- provider 缺少某項能力時，應明確提示、跳過該功能或 fail-closed；禁止靜默換用錯誤數據或錯誤口徑。
- 插件加載失敗、字段缺失和空數據必須有隔離測試，不能導致應用啟動失敗或其他數據源不可用。
- 新增數據源適配應同步更新 `docs/custom-data-source.md` 或 `docs/plugin-development.md` 中對應契約。

## 5. 領域專項要求

### 5.1 策略

- 內置策略和自定義策略的基礎參數、策略參數、過濾、評分和結果結構應走統一契約。
- 參數修改後要檢查：持久化內容、運行時實例、策略結果緩存、監控實例和前端查詢是否全部更新。
- 創建策略的表單狀態必須在每次打開時初始化；編輯態數據不得洩漏到新建態，關閉或創建成功後不能殘留上一個策略 ID。
- 策略 ID 是穩定標識。重命名、複製、導入和刪除必須分別處理 ID 衝突，不能靠覆蓋文件解決。
- AI 生成策略的 META 必須經過結構化解析和規範化；兼容歷史格式時保留舊格式讀取能力，但新輸出使用當前規範。
- 評分字段必須有確定的數據來源或臨時計算路徑。缺少輸入時應返回明確的不可計算狀態，不能把空值偽裝成零分。
- 刪除策略涉及用戶文件，必須校驗目標範圍並 fail-closed；不得允許路徑穿越或刪除策略目錄之外的文件。

### 5.2 監控與通知

- 監控計算應複用標準化實時行情和統一規則，不為頁面、語音、飛書、企業微信分別計算。
- 漲跌幅、漲跌停和均價穿越必須明確數據口徑、交易時段、資產類型和閾值方向。
- 分鐘信號只有在 provider 聲明分鐘數據能力且標的進入訂閱池後才啟用；無權限時給出明確狀態。
- 行情回調線程中不得執行全量歷史重算、同步 Webhook、慢磁盤掃描等長耗時任務。
- 通知文案使用用戶可理解的中文名稱，不洩漏內部枚舉名、模塊名或調試字段。
- 同一事件的去重、冷卻和恢復狀態必須可測試，重啟後的行為要與持久化設計一致。

### 5.3 回測、優化與步進優化

- 信號生成和成交模擬是兩個階段。必須明確區分信號時間、可成交時間和使用的價格。
- 嚴禁未來函數：交易日只能使用當時已經公開且可獲得的數據。
- 股票規則需保持 T+1、手續費、滑點、漲跌停不可成交等約束；新成交選項不得繞過這些規則。
- 日線回測不能聲稱“信號觸發瞬間成交”。分鐘精確成交只有在分鐘數據完整覆蓋目標窗口時才能啟用。
- 賣出信號的“當日收盤”“次日開盤”“次分鐘開盤”等選項必須分別測試時間對齊和邊界數據缺失。
- 優化和步進優化共享矩陣時，矩陣必須覆蓋每個 fold 的完整窗口；緩存鍵必須包含會改變矩陣結果的輸入。
- 修改選股、評分或排序邏輯時，同時核對歷史策略結果、回測交易記錄和 UI 展示是否使用同一候選集和排序方向。

## 6. 緩存、併發與性能

項目同時存在倉庫緩存、enriched 歷史緩存、實時聚合、篩選緩存、策略結果緩存、回測矩陣緩存、mtime 緩存、SSE 和前端 TanStack Query。修改讀寫路徑時必須列出受影響的緩存層。

### 6.1 緩存一致性

- 寫數據後同步檢查：持久化文件、內存緩存、generation/version、SSE 事件和前端 query invalidation。
- 不能只保證“文件已寫入”，卻繼續返回舊內存對象。
- 多步刷新優先構建新快照後原子替換，避免 UI 在刷新期間短暫變成空列表或零結果。
- 緩存鍵必須覆蓋資產類型、週期、日期、策略參數和其他會改變結果的維度。
- 失效範圍應精確；不能為了修復單條數據而無條件清空所有歷史緩存。

### 6.2 併發安全

- API、後台 worker 和實時行情線程共享字典或緩存時，使用鎖、不可變快照或原子替換。
- 不在持鎖期間執行網絡請求、磁盤全量掃描或重計算。
- 取消、超時和應用關閉路徑必須釋放資源，後台線程不得阻止進程退出。

### 6.3 性能約束

- 列表頁避免 N+1 API、每卡片獨立請求和重複全量策略計算，優先批量接口及共享查詢。
- Polars 計算優先表達式和批處理，避免逐行 Python 循環。
- 實時熱路徑不得隨歷史數據量線性增長；需要歷史窗口時只讀取必要範圍。
- 無規則、無訂閱者或 provider 無能力時，不執行對應實時計算。
- 聲稱“提升性能”的 PR 必須給出同一數據規模、同一環境下的修改前後數據，至少包括耗時或請求數。

## 7. 前端改動要求

- 複用 `frontend/src/lib/api.ts` 中的請求封裝和類型，禁止組件直接散落請求地址。
- TanStack Query 查詢鍵使用集中定義；資產類型、日期、週期等影響結果的參數必須進入查詢鍵。
- 切換股票、ETF、日期或策略後，不得把上一上下文的緩存結果當作當前結果。
- 實時刷新保留上一份有效數據直到新數據就緒，避免列、計數和列表閃爍消失。
- 彈窗打開和關閉必須正確重置臨時表單狀態；新建態與編輯態分離。
- 公共標籤、個股詳情、成分股等跨頁面行為使用共享組件，不在多個頁面複製交互。
- 新增交互必須覆蓋加載、空數據、錯誤、禁用和無權限狀態。
- 改動應在常用桌面和窄屏尺寸下檢查文字截斷、遮擋、滾動區域和彈窗可操作性。
- 不用前端補丁掩蓋後端單位或契約錯誤；契約問題應在數據邊界修正。

## 8. 安全、兼容與用戶數據

- 不提交 `data/`、密鑰、Token、Webhook、用戶策略、日誌、數據庫、Parquet 樣本和機器絕對路徑。
- 日誌和異常響應不得包含授權信息、完整配置或用戶敏感數據。
- 文件刪除、覆蓋、遷移和批量回算屬於高風險操作：限制目標目錄、驗證路徑、提供清晰失敗原因，並保持 fail-closed。
- 不用不安全的字符串拼接執行 SQL、Shell 或動態 Python。
- 配置、策略 JSON、Parquet schema 和 API 字段變化默認需要向後兼容。新增字段優先提供默認值，讀取歷史數據時允許字段缺失。
- 必須改變既有行為時，在 PR 中列出遷移方式、降級行為和影響範圍。
- Windows、macOS、Linux 和 Docker 的路徑、編碼、回收站及權限行為不同；涉及文件系統時至少審查跨平台失敗路徑。

## 9. 驗證矩陣

根據改動範圍執行最小但充分的驗證。以下是最低要求，不是上限。

| 改動類型 | 最低驗證 |
| --- | --- |
| 後端純函數、規則或 bug 修復 | 對應定向 `pytest`，包含復現用例和邊界用例 |
| API 契約 | service 測試 + API 測試；檢查成功、無數據和錯誤響應 |
| 數據源或插件 | provider 契約測試、缺少能力、空數據、字段/單位標準化測試 |
| 指標、復權、換手率、漲跌停 | 固定樣本數值斷言，覆蓋歷史邊界和降級路徑 |
| 策略或監控 | 參數變更、緩存失效、資產切換、重啟/併發相關路徑 |
| 回測 | 信號日與成交日、T+1、費用、滑點、不可成交、數據缺口 |
| 緩存或性能 | 命中/失效測試、併發快照測試；必要時提供前後基準 |
| 前端組件或頁面 | `pnpm build`，並手工檢查加載、空、錯、切換和實時刷新狀態 |
| 前後端契約同時變化 | 後端定向測試 + 前端構建 + 對應頁面聯調 |
| 文件、Docker 或跨平台邏輯 | 正常路徑、權限/能力缺失和目標不可用的失敗路徑 |

常用命令：

```bash
cd backend
uv run pytest tests/path/to/test_x.py -q
uv run ruff check app/path.py tests/path.py

cd frontend
pnpm build

git diff --check
```

- 後端至少運行受影響模塊的測試，不應只運行新加的單個測試。
- 新增文件和本次改動不得引入 Ruff 告警。不要為清理歷史告警而在功能 PR 中全倉格式化。
- 前端任何 TypeScript、組件、樣式或 API 類型改動至少執行一次 `pnpm build`。
- 提交前必須運行 `git diff --check`，並檢查最終 diff 不含調試代碼和無關文件。

## 10. PR 提交要求

一個可審查的 PR 應聚焦一個問題。功能、無關重構、批量格式化和依賴升級不得混在一起。

PR 描述必須包含：

1. **問題與復現**：原行為、復現條件和影響用戶。
2. **根因**：具體到調用鏈、數據契約或狀態生命週期，不能只寫“修復 bug”。
3. **解決方案**：改動模塊、關鍵取捨，以及為什麼沒有破壞插件化或現有口徑。
4. **兼容性**：對歷史配置、策略、緩存、Parquet、API、數據源和不同資產類型的影響。
5. **性能**：是否進入實時或列表熱路徑；有性能主張時附前後數據。
6. **驗證結果**：列出實際執行的命令和結果，不寫計劃執行的測試。
7. **界面證據**：有可見變化時提供修改前後截圖或短視頻，並覆蓋淺色/深色或窄屏中相關場景。
8. **風險與回滾**：剩餘風險、降級行為和可行的回滾方式。

提交前作者自檢：

- [ ] 改動只解決 PR 描述的問題，沒有夾帶無關整理。
- [ ] 已檢查完整調用鏈和所有受影響緩存。
- [ ] 已核對單位、復權、交易日、時區和資產類型。
- [ ] 未繞過 provider abstraction，缺少能力時行為明確。
- [ ] 歷史配置和舊數據仍可讀取，或已說明遷移方案。
- [ ] 實時線程、列表請求和批處理沒有明顯性能退化。
- [ ] 錯誤路徑 fail-closed，不會靜默返回錯誤金融結果。
- [ ] 測試能在未修復代碼上失敗，並在修復後通過。
- [ ] 已執行適用的測試、構建、Ruff 和 `git diff --check`。
- [ ] 最終 diff 不含敏感數據、本地路徑、調試輸出和生成文件。

## 11. PR 複審流程

作者自審和維護者複審均按以下順序進行，避免只看頁面效果或新增代碼：

1. **確認問題**：根據描述和測試復現舊行為，確認 PR 解決的是同一個問題。
2. **檢查邊界**：確認代碼位於正確模塊，沒有反向依賴、平行實現或 API 層重計算。
3. **核對數據契約**：檢查單位、復權、日期、時區、資產類型、空值和排序方向。
4. **核對插件化**：檢查是否依賴 provider 能力，非 TickFlow 數據源能否工作或明確降級。
5. **跟蹤狀態變化**：從寫入一路檢查持久化、緩存、generation、SSE 和前端查詢失效。
6. **檢查兼容性**：使用歷史配置、舊策略 JSON、舊 Parquet schema 和缺字段數據驗證。
7. **檢查性能和併發**：定位是否進入實時、列表或全量掃描熱路徑，檢查鎖和原子替換。
8. **檢查失敗路徑**：網絡失敗、無權限、空數據、能力缺失和部分寫入時不得產生錯誤狀態。
9. **評估測試質量**：測試必須斷言業務結果，覆蓋負例和邊界，不能只斷言接口返回 200。
10. **檢查最終 diff**：按文件逐項確認必要性，排除無關格式化、敏感信息和臨時文件。
11. **給出結論**：明確寫出“可合併”“修改後合併”或“不建議合併”，區分阻斷項和建議項。

問題嚴重級別：

| 級別 | 定義 | 合併要求 |
| --- | --- | --- |
| `P0` | 數據破壞、安全漏洞、可能導致嚴重錯誤交易結果 | 禁止合併，修復並完整複審 |
| `P1` | 核心流程不可用、錯誤金融口徑、未來函數、廣泛迴歸 | 禁止合併，修復並補回歸測試 |
| `P2` | 明確功能錯誤、兼容性問題或明顯性能退化 | 修改後合併 |
| `P3` | 維護性、局部體驗、文檔或測試補強建議 | 可不阻斷，但應記錄後續處理方式 |

複審意見應包含文件和行號、觸發條件、實際影響及建議修改方向。不要只寫“這裡可能有問題”或直接給出沒有證據的結論。

### 11.1 追加提交後的複審

- 先逐項核對上輪每個阻斷問題是否在代碼和測試中真正解決，不以提交者回復“已修改”為依據。
- 查看新增 commit 便於理解修復意圖，但最終必須重新審查 PR 相對目標分支的完整 diff，不能只看最後一次提交。
- 重新執行受影響測試，並檢查修復是否引入新的單位、緩存、兼容性、性能或插件化問題。
- 已解決的問題明確標記為已驗證；仍存在的問題引用當前最新行號並說明缺失內容。
- 原阻斷項全部解決且沒有新增阻斷項後，才能把結論改為“可合併”。

### 11.2 推薦的複審輸出格式

```text
結論：可合併 / 修改後合併 / 不建議合併

阻斷問題：
- [P1/P2] 文件:行號 - 觸發條件、實際影響、修改要求

非阻斷建議：
- [P3] 文件:行號 - 建議及理由

已驗證：
- 執行的測試或構建命令及結果
- 上輪問題的逐項複核結果

剩餘風險：
- 未覆蓋的平台、數據源、數據規模或場景
```

沒有發現問題時也應明確寫“未發現阻斷問題”，並說明尚未驗證的範圍，不能用“看起來沒問題”代替結論。

## 12. 不得合併的情況

出現以下任一情況，PR 不應合併：

- 無法說明根因，修改只是增加延時、重試、強制刷新或吞掉異常來掩蓋現象。
- 混用百分比、復權價、自然日和交易日，或無法證明金融計算口徑。
- 通用功能直接綁定單一數據源，且沒有能力檢測或明確降級。
- 寫入後存在已知緩存不一致，需用戶重啟或手動清數據才能正確顯示。
- 回測引入未來數據、繞過成交約束，或歷史結果和交易記錄使用不同選股邏輯。
- 實時線程增加阻塞網絡請求、全量歷史計算或無界任務。
- 破壞歷史策略、配置、數據文件或 API，且沒有兼容或遷移方案。
- 文件操作可能越界刪除、默認覆蓋用戶數據，或失敗後繼續執行。
- 測試沒有復現問題、只覆蓋正常路徑，或必要測試/前端構建失敗。
- PR 包含密鑰、用戶數據、本地數據目錄、機器絕對路徑或大量無關改動。
- 對評審提出的 `P0`、`P1`、`P2` 問題尚未處理或驗證。

## 13. AI 編碼代理額外規則

- 開始前先讀取本文件、目標模塊、相鄰實現、調用方和相關測試。
- 修改前用簡短計劃寫明每一步及驗證方式；簡單改動可只寫一句。
- 不把猜測當成事實。無法從代碼、測試或文檔確認的業務口徑，應明確詢問。
- 不因用戶要求“直接修復”就跳過復現、根因分析和迴歸驗證。
- 不自動執行提交、推送、合併、刪除文件、數據庫遷移或批量數據覆蓋；需要時按當前會話權限規則取得明確確認。
- 不覆蓋工作區已有改動。遇到同文件並行修改時先讀取最新內容並保留雙方邏輯。
- 不虛構測試結果、性能數據、GitHub 狀態或線上行為。未運行的驗證必須明確標註。
- 審查 PR 時先報告問題，按嚴重級別排序並引用文件行號；最後再給合併建議和簡短總結。
- 完成後檢查 `git diff` 和 `git status`，準確說明改了什麼、驗證了什麼、仍有什麼風險。

遵循本指南的衡量標準不是文檔寫得多，而是：改動範圍更小、金融口徑有證據、插件邊界不被繞過、測試能真實復現問題、複審結論可執行，且相同問題不再反覆返工。
