# Nanachi 的台股監控看板 操作說明書

> 適用版本：以面板「設定 → 系統設定 → 關於」顯示的版本為準。
>
> 文件用途：說明安裝、第一次啟動、各頁面操作、通知推播設定、資料來源與更新方式。
>
> 風險提示：本專案用於資料整理與研究，不構成任何投資建議。歷史資料不代表未來表現，實際決策與風險由使用者自行承擔。

## 目錄

- [1. 這是什麼](#1-這是什麼)
- [2. 安裝與啟動](#2-安裝與啟動)
- [3. 第一次啟動](#3-第一次啟動)
- [4. 核心使用流程](#4-核心使用流程)
- [5. 看板](#5-看板)
- [6. 台股選股](#6-台股選股)
- [7. 自選股](#7-自選股)
- [8. 多股比較](#8-多股比較)
- [9. 個股頁面與日 K](#9-個股頁面與日-k)
- [10. 監控中心](#10-監控中心)
- [11. LINE Messaging API 推播](#11-line-messaging-api-推播)
- [12. Telegram Bot 推播](#12-telegram-bot-推播)
- [13. AI 設定](#13-ai-設定)
- [14. 資料來源：TWSE / TPEx 官方公開資料](#14-資料來源twse--tpex-官方公開資料)
- [15. 歷史日 K 與 GitHub Release 資料包](#15-歷史日-k-與-github-release-資料包)
- [16. 更新最新資料](#16-更新最新資料)
- [17. 其他設定](#17-其他設定)
- [18. 常見問題](#18-常見問題)
- [19. 資料與隱私](#19-資料與隱私)

---

## 1. 這是什麼

這是一套自行架設、以本機資料為主的台股看盤與監控面板，資料來自台灣證券交易所（TWSE）與證券櫃檯買賣中心（TPEx）的公開資料，不需要任何付費行情 API Key 就能開始使用。

面板提供五個主要頁面，對應一條使用流程：

| 頁面 | 路徑 | 做什麼 |
| :--- | :--- | :--- |
| 看板 | `/` | 今天大盤強不強、產業誰強誰弱、自選股怎麼動 |
| 台股選股 | `/taiwan-screener` | 從全市場依條件找出符合的標的 |
| 自選股 | `/watchlist` | 管理自己追蹤的清單與分組 |
| 多股比較 | `/stocks/compare` | 多檔標的並列比較 |
| 監控中心 | `/monitor` | 盤中即時報價、五檔與規則觸發 |

另外還有個股頁面（`/stocks/<代號>`）與設定頁（`/settings`）。

**明確不做的事**：不預測漲跌、不推薦個股、不做自動下單。AI 功能只在你主動點擊時才會呼叫，且被限制在「解讀已呈現的數據」這個範圍內。

---

## 2. 安裝與啟動

三種方式擇一。所有方式的資料都留在本機。

### 方式 A：桌面安裝包（最簡單）

到專案 GitHub Releases 下載對應平台的安裝檔：

| 平台 | 檔案 |
| :--- | :--- |
| Windows x64 | `NanachiStockPanel-Setup-x64.exe` |
| macOS（Apple Silicon） | `NanachiStockPanel-macos-arm64.dmg` |
| Linux | `NanachiStockPanel-linux-x64.tar.gz` |

- **Windows**：雙擊執行，依安裝精靈操作，不需要系統管理員權限。預設安裝到 `D:\NanachiStockPanel`，沒有 D 槽時會自動改到使用者目錄。安裝完成後會建立桌面與開始功能表捷徑。
- **macOS**：只提供 Apple Silicon（M 系列晶片）版本。開啟 dmg 後把應用程式拖進 Applications 資料夾，首次啟動需按右鍵再選「開啟」以繞過 Gatekeeper。
- **Linux**：解壓縮後執行可執行檔。

使用者資料存放在安裝目錄下的 `data/` 子資料夾，解除安裝後重新安裝不會遺失（解除安裝時會另外詢問是否一併刪除）。

### 方式 B：Docker

需要先安裝 Docker 與 Docker Compose。

```bash
cp .env.example .env
docker compose up --build
```

啟動後開啟 <http://localhost:3018>。

### 方式 C：原始碼開發模式

需要 Python 3.11 以上、Node 20 以上、[`uv`](https://docs.astral.sh/uv/) 與 `pnpm`。

```bash
cp .env.example .env
./dev.sh                   # Windows 用 .\dev.ps1
```

腳本會自動檢查並安裝相依套件、釋放被占用的連接埠，然後同時啟動前後端：

- 後端 <http://localhost:3018>
- 前端 <http://localhost:3011>

### 需要設定什麼

日常使用不需要設定任何 API Key。`.env` 的常用項目：

| 設定項 | 說明 |
| :--- | :--- |
| `PORT` | 後端服務連接埠，預設 `3018` |
| `HOST` | 監聽位址，容器或公網部署用 `0.0.0.0`，只給本機用可設 `127.0.0.1` |
| `DATA_DIR` | 資料存放目錄，預設 `./data` |
| `AUTH_PASSWORD` | 首次啟動時預置存取密碼（選填，只在尚未設過密碼時生效） |
| `AI_*` | AI 功能的位址、模型與金鑰，也可以之後在面板裡設定 |

完整設定項見 [docs/configuration.md](./docs/configuration.md)，部署與存取密碼見 [docs/deployment.md](./docs/deployment.md) 與 [docs/deploy-password.md](./docs/deploy-password.md)。

---

## 3. 第一次啟動

### 3.1 設定存取密碼

如果面板要放在公司網路或公網上，第一次開啟會要求設定存取密碼。密碼只在首次設定時可以透過本機或內網設定，之後要改密碼請在面板內操作。建議至少 6 位。

只在自己電腦上用、沒有對外開放時，可以不設密碼。

### 3.2 走完引導

首次進入會跳到引導流程，共四步：

1. **使用須知** — 說明這是個人開源專案、與任何商業資料服務沒有官方關聯。
2. **歡迎** — 簡介台股選股、個股分析、多股比較、本機資料優先與選配的 AI 研究功能。
3. **台股資料狀態** — 顯示日 K、三大法人、融資融券三類資料目前的最新日期。
4. **完成** — 進入面板。

引導過程中不需要填任何金鑰，也可以直接略過。

### 3.3 準備歷史日 K

引導完成後，建議先到「設定 → 資料來源」把歷史日 K 資料庫準備好，選股頁的技術面條件與個股頁的歷史 K 線都依賴它。做法見[第 15 節](#15-歷史日-k-與-github-release-資料包)。

---

## 4. 核心使用流程

面板的側邊欄順序就是建議的使用順序：

```
看市場  →  找股票  →  加自選  →  多股比較  →  設定監控
 看板      台股選股    自選股     多股比較      監控中心
```

一個典型的日常流程：

1. 開「看板」看今天全市場漲跌家數、哪些產業強、自選股怎麼動。
2. 到「台股選股」用常用篩選或自訂條件掃出候選名單。
3. 把有興趣的標的加進「自選股」，需要的話分到不同分組。
4. 對幾檔難以取捨的標的用「多股比較」並列看數據。
5. 在「監控中心」或個股頁對選定標的建立價格、漲跌幅或量能規則，命中時推播到 LINE 或 Telegram。

側邊欄的順序與顯示與否可以在「設定 → 選單設定」自行調整。

---

## 5. 看板

路徑 `/`。開啟面板的第一個畫面，由幾個區塊組成。

### 今日市場強弱

顯示當日交易日期，以及全市場的上漲、平盤、下跌家數。系統依上漲家數比例給一個標籤：

| 上漲家數比例 | 標籤 |
| :--- | :--- |
| ≥ 55% | 偏強 |
| 45% ～ 55% | 中性 |
| ≤ 45% | 偏弱 |

這是對已發生數據的描述，不是對後續走勢的預測。若當日資料尚未完整，區塊上方會出現提醒並標示前一交易日。

### 產業強弱

把當日各大類股的表現排序，分成「最強」與「最弱」兩欄列出。點產業名稱可以帶到選股頁做進一步篩選。

### 自選股動態

列出自選清單目前的即時變化。還沒加過任何標的時會顯示提示。

### 台股資料狀態

用三列顯示日 K、三大法人、融資融券的資料日期與新鮮度：

| 標記 | 意思 |
| :--- | :--- |
| 最新 | 已更新到最近一個交易日 |
| 過期 | 本機資料落後，需要更新 |
| 尚無資料 | 還沒下載過這類資料 |

旁邊有到台股選股、多股比較、自選股的快速連結。

### 監控中心即時信號

顯示最近的觸發記錄，依來源分為策略、訊號、價格、異動、板塊幾類，點事件可以直接跳到該標的的日 K。右上角的按鈕會進入監控中心。

---

## 6. 台股選股

路徑 `/taiwan-screener`。從全市場上市櫃標的中，依價格、成交、技術面與法人條件篩選。

### 資料更新狀態

頁面頂端顯示日線、法人、資券三類資料各自的資料日期，以及符合目前條件的檔數。展開後可以看到每類資料是「最新」還是「無資料」，以及自動更新排程時間（交易日 16:30，Asia/Taipei）。

### 台股全市場量化統計快照

當日全市場的整體數字，包含：

- 全市場漲跌家數（上漲 / 下跌 / 平盤）
- 漲跌停家數（只計有法定價格限制的標的）
- 交易所成交額（上市、上櫃分列）
- 外資買賣超總計
- 投信買賣超總計
- 融資餘額增減與全市場券資比

### 台股產業類股輪動

以產業為單位的統計表，欄位包含標的數、漲跌平家數、上漲比、中位漲跌、成交金額與成交佔比、5 日與 20 日相對強弱、外資與投信買賣超。可依成交金額、RS 5D、RS 20D、今日漲跌中位數、上漲家數比例、外資買賣超、投信買賣超排序，也可切換升冪或降冪。點產業名稱會直接把該產業套進下方的篩選條件。

### 異常訊號

系統會掃出當日觸發客觀異常條件的標的，可依訊號類型篩選：

| 訊號 | 條件 |
| :--- | :--- |
| 爆量 | 成交量達基準 2 倍以上 |
| 成交額放大 | 成交金額達基準 2 倍以上 |
| 價格異動 | 漲跌幅達 5% 以上 |
| 外資異常買賣超 | 外資買賣超顯著偏離 |
| 投信異常買賣超 | 投信買賣超顯著偏離 |
| 融資激增 | 融資餘額大幅增加 |
| 融券激增 | 融券餘額大幅增加 |
| 券資比驟升 | 券資比明顯跳升 |
| 價量/法人背離 | 價格與量能或法人流向方向不一致 |
| 產業相對強弱異常 | 個股相對所屬產業表現異常 |

這些是純規則判定的事實描述，系統不會據此給推薦。

### AI 自然語言條件解析

如果已設定 AI，可以直接用中文描述條件，例如「找外資買超 1000 張以上、券資比低於 5% 的股票」或「100 元以下的 ETF」。

這是**純翻譯層**：AI 只負責把你的句子轉成篩選欄位，解析結果會先以預覽呈現，你確認後才填進下方表單，再由本機執行篩選。AI 不會直接產出名單。無法量化的條件會被列在「未支援或無法量化條件」，不會偷偷忽略。

### 常用篩選

五個一鍵套用的組合，本質是幫你填好下方欄位：

| 名稱 | 實際條件 |
| :--- | :--- |
| 強勢股 | 站上 MA20 且 5 日動量 ≥ 2 |
| 放量股 | 量比 ≥ 2 |
| 法人買超 | 外資買超 ≥ 1000 張 |
| 突破轉強 | 同時站上 MA5 與 MA20 且當日收紅 |
| 熱門股 | 成交金額 ≥ 500 百萬 |

再點一次已套用的按鈕會清掉該組合設定的欄位。手動改動任一相關欄位時，該組合的選取外觀會自然解除。

### 篩選條件

基本條件預設就看得到，包含漲跌幅區間、價格區間、外資買超張數、成交金額等。點「進階條件」可以展開技術面、法人、籌碼與融資券的細部條件。

---

## 7. 自選股

路徑 `/watchlist`。管理自己追蹤的標的清單。

### 加入與移除

- 頁面上方的搜尋框輸入代號或名稱即可搜尋並加入。
- 個股頁、選股結果、比較頁都能直接把標的加進自選。
- 移除有兩種：「移出目前分組」只把標的從這個分組拿掉、仍留在自選中；「從自選移除」則完全移除。
- 「清空自選」會清掉整份清單，執行前有確認對話框。

### 分組

一檔標的可以同時屬於多個分組。分組可以自訂名稱與顏色，也可以拖曳排序、把標的移到頂部。

### 檢視方式

表格與卡片兩種檢視可以切換。表格模式支援自訂欄位：點「自訂欄位」可以決定要顯示哪些欄位，例如代碼/名稱、現價、漲跌幅、換手率、訊號、連續漲停、連續跌停、60D 動量等，欄位設定會存在瀏覽器本機。

清單可依板塊、風險警示（隱藏簡稱含 ST 標記的標的）等條件過濾，也可以用搜尋框在自選內快速定位。每一列可以展開迷你日 K 或分時走勢。

---

## 8. 多股比較

路徑 `/stocks/compare`。把多檔台股放在同一張表裡並列比較。

### 選擇標的

用搜尋框輸入代號或名稱加入，可以加入多檔。上方會列出目前已選的標的，可以逐一移除。

### 確定性客觀比較

這張表是**純本機計算**的，不經過 AI。可以指定比較日期，留空則用最新交易日。欄位包含：

- 標的類型（股票或 ETF）
- 收盤價
- 5 日報酬率
- 20 日報酬率
- 外資買賣超（1 日）
- 本益比（PE）
- 異常訊號數

### AI 客觀比較（選配）

表格下方有一個 AI 解讀區塊，**不會自動呼叫**，要按下按鈕才會執行。它只依上方那張表的數據做客觀敘述，分成量價與技術位階、三大法人籌碼、融資融券、基本面或 ETF 屬性、異常訊號幾個面向，並附上重點觀察、數據揭示的風險特徵與系統資料涵蓋度說明。

設計上它被限制在既有事實範圍內，不會給出優劣排序或投資建議。

---

## 9. 個股頁面與日 K

路徑 `/stocks/<代號>`，例如 `/stocks/2330.TWSE`。從自選、選股結果、比較頁或搜尋框都能進來。

代號需要符合台股標準格式，上市用 `.TWSE`、上櫃用 `.TPEX`，例如 `2330.TWSE`、`8069.TPEX`。

### 身份與規則

頁面頂端顯示標的名稱、上市或上櫃、股票或 ETF，以及：

- **漲跌幅限制規則** — 該標的適用的漲跌停規則，無限制的商品會特別標示。
- **所屬大盤基準** — 用於相對強弱計算的基準指數。

權證、ETN 等未支援監控的商品會顯示提示，只提供基本身份資訊與參考報價。

### 即時報價

昨收、今開、最高、最低、成交量與行情時間。成交量可以在「張」與「股」之間切換。資料過期時會標示「⚠️ 資料已過期」。

### 歷史日 K 線與成交量

主圖顯示歷史日 K 與成交量，可切換 1 個月、3 個月、6 個月、1 年四種區間。這張圖的資料來自本機歷史日 K 資料庫，沒有下載過資料包時會是空的。

### 五檔即時盤口

盤中顯示委買委賣五檔，單位可切換張或股。沒有報價時會顯示「無買盤報價」或「無賣盤報價」。五檔能力是否可用取決於目前的資料來源設定，狀態可在「設定 → 即時監控」查看。

### 三大法人買賣超

分列外資及陸資、投信基金、自營商合計與三大法人合計。

### 融資融券餘額與變化

融資餘額與增減、融券餘額與增減、券資比。

### 核心籌碼滾動因子

近 5 日的滾動統計，包含外資 5 日累計、投信 5 日累計、自營商 5 日累計、融資變動量與最新券資比因子。

### 標的監控規則

頁面下方列出這檔標的目前已建立的監控規則，右上角的「新增監控」可以直接為這檔標的建立規則，欄位會預先帶入。

---

## 10. 監控中心

路徑 `/monitor`。盤中即時行情與規則觸發都在這一頁。

### 版面

- **上方** — 台股即時行情與五檔盤口。
- **左下** — 台股觸發記錄，預設顯示近 7 日、最多 500 筆。進入本頁時未讀徽標會清零，之後新產生的記錄會閃爍提示。
- **右下** — 台股規則管理，列出目前所有規則，可新增、編輯、刪除。

### 建立規則

點右下角的「＋」或從個股頁的「新增監控」進入。可設定的欄位：

**標的** — 直接輸入 `2330.TWSE` 這類標準代號，或用搜尋框輸入代號或名稱挑選。搜尋結果會標示無漲跌幅限制的商品，以及未支援監控的權證與 ETN。

**規則名稱** — 例如「台積電突破 2500 元」。

**規則類型** — 八選一：

| 類型 | 單位 | 觸發條件 |
| :--- | :--- | :--- |
| 價格高於 | TWD | 成交價突破或高於指定價格 |
| 價格低於 | TWD | 成交價跌破或低於指定價格 |
| 漲幅高於 | % | 相較昨收的上漲幅度超過門檻 |
| 跌幅低於 | % | 相較昨收的下跌幅度超過門檻 |
| 成交量高於 | 股（可切換張） | 累積成交量超過門檻 |
| 成交量異常放大 | 倍數 | 目前量能超過基準參考量的倍數 |
| 接近漲停 | % | 距官方漲停價的距離百分比 ≤ 門檻 |
| 接近跌停 | % | 距官方跌停價的距離百分比 ≤ 門檻 |

「接近漲停」與「接近跌停」對無漲跌幅限制的商品不適用，選單中會標示為不可用。

**冷卻時間（秒）** — 同一條規則兩次觸發之間的最短間隔，避免同一波行情連續轟炸。

**遲滯防抖回調** — 價格在門檻附近來回震盪時，要回退多少才算重新離開觸發區。留空表示不設。

**嚴重級別** — 一般（INFO）、提醒（WARN）、緊急（CRITICAL），影響通知的呈現。

規則儲存時後端會檢查標的與商品限制，不合法會直接拒絕並說明原因。

### 推播

規則命中後可以把告警送到外部管道。要先在「設定 → 即時監控 → 推播管道」設定 LINE 或 Telegram，並勾選要作為新建規則預設推播的管道。

---

## 11. LINE Messaging API 推播

在「設定 → 即時監控 → 推播管道」設定。需要兩個值：**Target ID** 與 **Channel Access Token**。

### 取得步驟

1. **建立 LINE Official Account** — 前往 LINE Official Account 平台註冊並建立官方帳號。
2. **啟用 Messaging API** — 登入後台管理頁面，在「設定 > Messaging API」分頁點選啟用。
3. **取得 Channel Access Token** — 登入 LINE Developers Console，在頻道的「Messaging API」頁籤發行並複製長期 Channel Access Token。
4. **取得 Target ID** — 這是接收對象的識別碼。
5. **填入設定** — 回到面板填入 Channel Access Token 與 Target ID。
6. **儲存並驗證** — 按「儲存」後再按「測試通知」，確認能收到測試訊息。

### 關於 Target ID

**Target ID 不等於一般的 LINE ID**，不能填個人自訂的好友搜尋帳號或行動條碼。

- **要傳給自己**：登入 LINE Developers Console，進入該頻道的「Basic settings」頁籤，捲到最下方複製「Your user ID」，是一串以 `U` 開頭的 33 碼字串。
- **要傳給其他人或群組**：其他人的 User ID（`U…`）或群組 ID（`C…`）通常需要透過設定 webhook event（例如好友訊息或加入群組事件）動態取得。

### 注意事項

- Token 欄位留空儲存時會保留目前已存的值，不需要每次重貼。
- 畫面上只會顯示遮罩後的 Token。
- **Channel Access Token 視同密碼，請妥善保管，不要分享給他人。**

---

## 12. Telegram Bot 推播

同樣在「設定 → 即時監控 → 推播管道」設定。需要兩個值：**Chat ID** 與 **Bot Token**。

### 取得步驟

1. **在 Telegram 找到 @BotFather** — 搜尋官方機器人管理員 `@BotFather` 並開啟對話。
2. **用 /newbot 建立 Bot** — 發送 `/newbot`，依引導設定 Bot 名稱與 username（username 結尾必須是 `bot`）。
3. **取得 Bot Token** — 建立完成後 BotFather 會回傳 API Token，格式類似 `123456:ABC-DEF...`。
4. **先傳訊息給這個 Bot** — 在 Telegram 搜尋剛建立的 Bot，按「Start」並隨便傳一則文字訊息。Bot 必須先收到訊息才能建立通訊通道。
5. **讀取 Chat ID** — 呼叫 `getUpdates`，在回傳的 JSON 裡展開 `result`，找到 `message.chat.id`。個人聊天是正整數，群組通常是以 `-` 開頭的負整數，例如 `-1001234567890`。
6. **填入設定** — 回到面板填入 Chat ID 與 Bot Token。
7. **儲存並驗證** — 按「儲存」後再按「測試通知」。

### 注意事項

- Token 留空儲存時會保留目前已存的值。
- **Bot Token 視同密碼，請妥善保管，不要分享給他人。**

---

## 13. AI 設定

在「設定 → AI 設定」。AI 是**選配**功能，不設定完全不影響選股、自選、監控與推播。

目前 AI 被用在兩個地方：選股頁的自然語言條件解析、多股比較頁的客觀比較解讀。兩者都要你主動觸發，系統不會在背景自動呼叫。

### 兩種預設

| 預設 | 說明 |
| :--- | :--- |
| 自訂 | 不自動帶入任何設定，API 位址、模型與金鑰全部手動填寫。任何 OpenAI 相容介面都可以用。 |
| OpenAI | 帶入 OpenAI 官方介面位址與預設模型，可另外設定模型支援的推理強度。 |

### 欄位

- **API 位址** — 例如 `https://api.openai.com/v1`，自訂供應商填其相容端點。
- **模型** — 模型 ID。
- **API Key** — 已設定過時，留空儲存代表不修改；畫面只顯示遮罩值。
- **推理強度** — OpenAI 專屬欄位。
- **User-Agent**、**最大輸出 tokens**、**Context window** — 進階微調，通常不用改。

填完可以按「測試」確認連線，成功會顯示連線成功與模型名稱。另外有「清空 AI 設定」可以把整組設定移除。

---

## 14. 資料來源：TWSE / TPEx 官方公開資料

在「設定 → 資料來源」。

預設且建議的來源是**台灣官方資料源（TWSE/TPEx）**，整合台灣證券交易所與證券櫃檯買賣中心的官方公開資料，提供日 K 歷史行情、即時報價、標的清單與基本面資訊，**不需要任何 API Key 即可開箱即用**。Yahoo Finance 與公開資訊站台作為備援與補充。

所有取得的資料都會在本機建立 Parquet / DuckDB 快取，查詢走本機，不受第三方付費 API 的頻率限制。

### 逐資料集切換來源

能力卡片上會列出各個資料集（日 K、即時報價、標的清單、基本面等）目前由誰提供。點資料集上的標籤可以個別指定提供方；沒有個別設定的資料集一律由台灣官方資料源提供。若你另外接了自訂 HTTP 資料源或插件，它們會出現在候選清單裡。

按「恢復預設」會把所有路由改回台灣官方資料源。

---

## 15. 歷史日 K 與 GitHub Release 資料包

即時報價可以隨時從官方端點抓，但歷史日 K 需要先在本機建立起來。逐日從官方端點回補要跑很久，所以專案把整理好的歷史資料放在 GitHub Release，讓你一次下載匯入。

### 目前提供的資料包

| 項目 | 內容 |
| :--- | :--- |
| Release tag | `data-daily-2026-09-10` |
| 檔案 | `nanachi-tw-daily-2024-01-02_to_2026-09-10.zip` |
| 資料範圍 | 2024-01-02 ～ 2026-09-10 |
| 大小 | 約 31 MB |
| 校驗 | 下載後自動比對 SHA256 |

### 下載步驟

到「設定 → 資料來源 → 台股歷史日 K 資料庫」。

還沒有資料時，卡片會標示「缺少歷史日 K」，點下載按鈕即可開始。任務會依序經過：

```
準備中 → 下載中 → 驗證校驗碼 → 解壓縮檔案 → 匯入資料庫 → 補充最新行情 → 已完成
```

「補充最新行情」這一步會自動把資料包結束日之後、到最近一個交易日之間的缺口補上，所以下載完成時資料就是最新的。

匯入完成後，卡片會顯示「資料已就緒」，以及最早交易日、最新交易日與涵蓋交易天數。

### 重新下載

已經有資料時仍可以重新下載資料包，執行前會出現確認。重新匯入會覆寫同日期的分區，資料包範圍以外的新分區與你自己的資料（自選、分組、監控規則、觸發記錄）都會保留。

---

## 16. 更新最新資料

### 自動更新

系統排程在**交易日 16:30（Asia/Taipei）**自動更新日線、法人與資券資料。只要面板當時是開著的，你不需要做任何事。

各類資料的最新日期可以在三個地方看到：看板的「台股資料狀態」、選股頁頂端的資料日期列、設定頁的歷史日 K 卡片。

### 手動更新

歷史日 K 卡片右上角有「更新到最新」按鈕，會立刻把本機資料補到最近一個交易日。適用情境：

- 面板關了幾天，回來時資料狀態顯示「過期」。
- 排程時間點電腦剛好沒開機。
- 剛下載完資料包想確認已補到今天。

### 資料狀態怎麼讀

| 標記 | 意思 | 該做什麼 |
| :--- | :--- | :--- |
| 最新 | 已更新到最近一個交易日 | 不用動作 |
| 過期 | 本機資料落後 | 按「更新到最新」 |
| 尚無資料 / 無資料 | 還沒下載過 | 先下載歷史資料包 |

盤後資料通常在收盤後一段時間才由交易所發布，收盤當下立刻查看有機會還是前一日的數字，這是正常的。

---

## 17. 其他設定

設定頁分成七個分頁。

| 分頁 | 內容 |
| :--- | :--- |
| 資料來源 | 台灣官方資料源、逐資料集路由、歷史日 K 資料庫 |
| AI 設定 | 見[第 13 節](#13-ai-設定) |
| 即時監控 | 輪詢間隔、重新整理間隔、五檔盤口狀態、推播管道 |
| 訊號庫 | 檢視內建訊號與管理自訂訊號 |
| 逾時設定 | 各類請求的逾時參數 |
| 選單設定 | 側邊欄項目的顯示與排序 |
| 系統設定 | 通知彈窗、音效、語音播報、快取、關於 |

### 即時監控

- **輪詢間隔** — 每輪拉取行情的間隔。有全市場與自選頁兩種模式，切換後說明文字會跟著變。資料同步進行中時輪詢會自動暫停，狀態列會顯示「執行中」「執行中（非交易時段）」或「已關閉」。
- **重新整理間隔** — 畫面重新取數的間隔。
- **五檔盤口** — 目前來源若不提供五檔，這裡會標示「五檔盤口不可用」，並提供「立即修正」嘗試切換到可提供的來源。
- **推播管道** — LINE 與 Telegram 的設定與測試，見第 11、12 節。

### 訊號庫

分成兩塊。**內建訊號**由系統在資料處理階段預先計算，唯讀，ID 以 `signal_` 開頭。**自訂訊號**由你自己定義，可以啟用或停用，在監控中以 `csg_` 開頭的 ID 使用。

### 系統設定

- **通知彈窗** — 同時顯示的通知數量上限（1 至 5），超出時捨棄最舊的。
- **音效** — 選擇提示音風格。
- **語音播報** — 選擇語音音色與語速（0.5 慢至 2.0 快）。建議優先選台灣繁體中文語音；系統沒有中文語音時會用系統預設。
- **快取** — 「清理並重新整理」會清掉前端快取後重載，畫面顯示異常時可以先試這個。
- **關於** — 顯示目前安裝的版本，以及前往 GitHub Releases 檢查更新的連結。

---

## 18. 常見問題

**Q：一定要申請什麼 API Key 才能用嗎？**
不用。預設的台灣官方資料源（TWSE/TPEx）不需要任何金鑰。只有想用 AI 功能時才需要自備 AI 服務的 API Key。

**Q：個股頁的 K 線圖是空的。**
歷史日 K 資料庫還沒建立。到「設定 → 資料來源 → 台股歷史日 K 資料庫」下載資料包，見[第 15 節](#15-歷史日-k-與-github-release-資料包)。

**Q：資料狀態一直顯示「過期」。**
按歷史日 K 卡片的「更新到最新」。如果按了還是過期，先確認今天是不是交易日，以及盤後資料是否已經發布；再確認網路能連到交易所端點。

**Q：五檔盤口沒有資料。**
先看「設定 → 即時監控」是否標示「五檔盤口不可用」。如果是，按「立即修正」；如果不是，確認該標的當下是否真的有委託掛單，權證與 ETN 等未支援監控的商品不提供五檔。

**Q：輸入代號查不到標的。**
代號要用標準格式，上市加 `.TWSE`、上櫃加 `.TPEX`，例如 `2330.TWSE`、`8069.TPEX`。也可以直接在搜尋框打中文名稱或數字代號，讓系統幫你補完。

**Q：監控規則存不進去，說操作被拒絕。**
最常見的原因是對無漲跌幅限制的商品設了「接近漲停」或「接近跌停」，或是選到權證、ETN 這類未支援監控的標的。錯誤訊息會說明具體原因。

**Q：規則一直重複觸發。**
把「冷卻時間」調長，或設定「遲滯防抖回調」，讓價格在門檻附近震盪時不會反覆觸發。

**Q：測試通知收不到。**
LINE 要確認 Target ID 是開發者 User ID（`U` 開頭 33 碼）或群組 ID（`C` 開頭），不是一般的 LINE ID。Telegram 要確認你已經先對 Bot 送過訊息，Chat ID 才會存在。兩者都要確認 Token 沒有過期或被撤銷。

**Q：AI 功能沒有反應。**
先到「設定 → AI 設定」按「測試」看連線是否成功。另外，AI 的兩個功能都要手動觸發：選股頁要按「解析條件」，比較頁要按 AI 解讀按鈕。

**Q：忘記存取密碼怎麼辦？**
見 [docs/deploy-password.md](./docs/deploy-password.md) 的重置說明。

**Q：換電腦時資料要怎麼搬？**
整個 `data/` 目錄複製過去即可，裡面包含行情資料、自選清單、分組、監控規則與觸發記錄。

**Q：升級版本會不會弄丟資料？**
不會。桌面版覆蓋安裝只更新程式檔案，`data/` 不在安裝清單內。Docker 與開發模式的資料在掛載的 `data/` 目錄，也不受影響。

---

## 19. 資料與隱私

### 資料放在哪裡

所有資料都在你自己的機器上：

| 內容 | 位置 |
| :--- | :--- |
| 行情、法人、資券等歷史資料 | `data/`（Parquet / DuckDB） |
| 自選清單與分組 | `data/` |
| 監控規則與觸發記錄 | `data/` |
| API Key 與推播 Token | 本機設定檔，畫面只顯示遮罩值 |
| 欄位配置、檢視模式等偏好 | 瀏覽器本機儲存 |

沒有任何使用者資料會被回傳給專案作者。

### 會對外連線的情況

面板只在這些情況連外：

1. 向 TWSE / TPEx 官方端點與備援站台取得公開行情資料。
2. 首次下載歷史日 K 資料包時連到 GitHub Release。
3. 你設定並啟用推播後，把告警送到 LINE 或 Telegram。
4. 你設定並主動觸發 AI 功能時，把相關數據送到你指定的 AI 服務。

其中第 3 與第 4 項要你自己設定才會發生。不設定就完全不會發生。

### 使用 AI 時要注意什麼

啟用 AI 代表你選定的那家 AI 服務會收到你送出的查詢內容與相關數據。要不要接受這件事，取決於你對該服務商的信任程度。不想讓任何資料離開本機，就不要設定 AI，其餘功能完全不受影響。

### 金鑰保管

Channel Access Token、Bot Token 與 AI API Key 都視同密碼。面板只顯示遮罩值，但它們仍以可還原的形式存在本機設定中。不要把設定檔或 `data/` 目錄公開分享，也不要提交到版本控制。

---

## 免責聲明

本專案為個人開源專案，由個人獨立維護，與台灣證券交易所、證券櫃檯買賣中心或任何商業資料服務均**無官方關聯**。

資料僅供學習與研究使用，**不構成任何投資建議**。資料正確性以各來源官方公告為準，系統不保證即時性與完整性。股市有風險，投資決策與後果由使用者自行承擔。
